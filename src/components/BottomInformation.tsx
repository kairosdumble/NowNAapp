import React from 'react';
import { View, Text } from 'react-native';
import styles from '../styles/MainScreenStyles.ts';

export default function BottomInformation() {
  return (
      <View style ={styles.bottomInformation}>
        <Text style ={styles.bottomInformationFont}>
        </Text>
      </View>
  );
}

